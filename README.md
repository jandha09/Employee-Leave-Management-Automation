# Employee-Leave-Management-Automation
Automates the full leave request lifecycle: employees submit requests through a form, the workflow checks their remaining balance, auto-approves requests within budget, and escalates anything over budget to their manager. Logs every request to Google Sheets and sends status notifications via email and Slack.
